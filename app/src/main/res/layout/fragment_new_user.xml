<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.heejae.tenniverse.persentation.home.newuser.NewUserViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/backgroundColor"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <ImageView
            android:id="@+id/iv_back"
            android:backgroundTint="@color/black"
            style="@style/style_back_btn"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/tv_title"
            app:layout_constraintBottom_toBottomOf="@id/tv_title"/>

        <TextView
            android:id="@+id/tv_title"
            style="@style/style_top_title"
            android:text="@string/new_user_list"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>


        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:orientation="vertical"
            android:layout_marginTop="30dp"
            android:layout_marginBottom="24dp"
            app:layout_constraintBottom_toTopOf="@id/btn_part"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintTop_toBottomOf="@id/tv_title"/>


        <Button
            android:id="@+id/btn_part"
            android:layout_marginEnd="16dp"
            android:text="@string/accept"
            android:enabled="@{viewModel.btnValidation.booleanValue()}"
            android:backgroundTint="@{viewModel.btnValidation.booleanValue() ? @color/green: @color/sub_light_gray}"
            style="@style/style_home_btn"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/btn_reject"/>

        <Button
            android:id="@+id/btn_reject"
            android:text="@string/reject"
            style="@style/style_home_btn"
            android:enabled="@{viewModel.btnValidation.booleanValue()}"
            android:backgroundTint="@{viewModel.btnValidation.booleanValue() ? @color/black_gray: @color/sub_light_gray}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/btn_part"/>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>